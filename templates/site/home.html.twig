{% extends 'base.html.twig' %}
{% block title %}Accueil{% endblock %}
{% block span %}<div id="myBurger" class="openMyBtn" onclick="openMyNav()"><span class="homeSpan"></span><span class="homeSpan"></span><span class="homeSpan"></span></div>{% endblock %}
{% block body %}
      <!-- Header -->
      <header class="masthead d-flex">
        
        <div class="container text-center my-auto">
            {% for message in app.flashes('sucess') %}
          <span class="badge badge-success" style="font-size:15px; font-weight:100;">{{ message }}</span>
      {% endfor %}
          <h1 class="mb-1 main_title_home">DÉVELOPPEUR WEB JUNIOR </h1>
          <hr>
          <h2 class="mb-5 my_name">
            BOKOLO N'DJALI Yannick
          </h2>
          <a class="btn btn-primary btn-xl js-scroll-trigger" href="{{ path('about') }}">Codons ensemble !</a>
        </div>
        <div class="overlay"></div>
      </header>
{% endblock %}
